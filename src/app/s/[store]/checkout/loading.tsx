import { CheckoutSkeleton } from "@/components/store/StoreSkeletons";

/* Skeleton de carga (spec §14.5) con los tokens del tema. */
export default function Loading() {
  return <CheckoutSkeleton />;
}
